## Summary
Update the three onboarding documents to document the RSS news feature and the two new CLI commands (`kryptos news`, `kryptos dryrun`).

## Acceptance Criteria
- [ ] `setup.sh`: add `feedparser` to the pip install step (it will already be in `requirements.txt` from Issue #242); no API key prompt needed — RSS is key-free
- [ ] `SETUP.md`: add new section **11. News Headlines Feed (Optional)**:
  - Explain that headlines are pulled from free public RSS (CoinDesk, Cointelegraph, Google News)
  - No API key required
  - Enable/disable via `news.enabled: true/false` in `config.yaml`
  - Describe `coin_names` map for customising Google News queries
  - Note: 30-min cache keeps fetch frequency low; `feedparser` dependency auto-installed by `setup.sh`
- [ ] `README.md`:
  - Add `kryptos news` and `kryptos dryrun` to CLI Commands reference table with usage examples
  - Add one-liner in the LLM Decision Cycle section: *"RSS headlines (CoinDesk, Cointelegraph, Google News) are injected per BUY/SELL pair into the cycle prompt when `news.enabled: true`"*

## Files
- `setup.sh`
- `SETUP.md`
- `README.md`

## Dependencies
- **Should follow** Issues #242, #243, #244, #245 being merged
