# Session Notes — 2026-04-09 (Part C)

## Changes

### 1. Disable Time-of-Day Trading Hours Guard (closes #121)

**Feature request:** Remove the 1-hour trading window restriction (03:00–04:00 SGT / 19:00–19:59 UTC) so the agent can trade at any hour of the day.

**Motivation:** The narrow 1-hour window was set for focused backtest testing (session_2026_04_08c / #105). It is no longer needed and blocks the vast majority of trading opportunities.

**Fix:**
- `config.yaml`: `allowed_trading_hours.enabled: false` (was `true`)
- `.claude/skills/trading-rules/SKILL.md`: Updated line 12 — time-of-day guard is disabled; volume floor still active
- `.claude/memory/project_kryptos.md`: Trading Hours section updated to reflect disabled state

**Note:** The volume dead zone check (`min_volume_ratio` / `rolling_volume_p15`) remains always-on in `signals.py` Hard Blocker 3, regardless of the `enabled` flag. Only the clock-based hour check in `risk_manager.py` is disabled.

---
