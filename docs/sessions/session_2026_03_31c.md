# Session Notes — 2026-03-31 (Part C)

## Changes

### Hardcoded qwen3.5:27b Fallbacks Updated

All remaining `qwen3.5:27b` hardcoded fallback strings replaced with `qwen2.5:14b` to match `config.yaml → llm.model`.
All code already reads from `config.yaml` — the hardcoded strings are only last-resort fallbacks.

| File | Change |
|---|---|
| `src/analysis/features.py` | `generate_post_trade_analysis` default param `qwen3.5:27b` → `qwen2.5:14b` |
| `kryptos.py` | `_make_nl_parser` fallback `qwen3.5:27b` → `qwen2.5:14b` |
| `src/agent/trading_agent.py` | `TradingAgent.__init__` fallback `qwen3.5:27b` → `qwen2.5:14b` |
| `src/agent/tools.py` | post-trade analysis fallback `qwen3.5:27b` → `qwen2.5:14b` |
| `src/cli/nl_parser.py` | `NLParser.__init__` default param `qwen3.5:27b` → `qwen2.5:14b` |

## Key Principle
All model references must read from `config.yaml → llm.model`.
The hardcoded default in each file is a last-resort fallback only — in normal operation config.yaml drives the model name.
To change the model in future: edit `config.yaml` only — no code changes needed.
