# Session Notes — 2026-03-30 (Part D)

## Changes

### Hardcoded Model Names Removed

All hardcoded `qwen2.5:14b` and `qwen2.5:7b` defaults replaced with `qwen3.5:27b`.
All code already reads from `config.yaml → llm.model` — the hardcoded strings were only fallback defaults.

| File | Hardcoded default removed |
|---|---|
| `src/cli/nl_parser.py` | `qwen2.5:7b` → `qwen3.5:27b` (NLParser default) |
| `src/agent/trading_agent.py` | `qwen2.5:14b` → `qwen3.5:27b` (TradingAgent default) |
| `src/agent/tools.py` | `qwen2.5:14b` → `qwen3.5:27b` (post-trade analysis default) |
| `src/analysis/features.py` | `qwen2.5:14b` → `qwen3.5:27b` (generate_post_trade_analysis default) |
| `kryptos.py` | `qwen2.5:14b` → `qwen3.5:27b` (_make_nl_parser default) |

## Key Principle
All model references must read from `config.yaml → llm.model`.
The hardcoded default in each file is a last-resort fallback only — in normal operation config.yaml drives the model name.
To change the model in future: edit `config.yaml` only — no code changes needed.
