# Session Notes — 2026-04-06 (Part D)

## Changes

### 1. S12.3.1: Breakeven Stop — Tests (closes #85)

**Feature context:** Breakeven stop logic was already committed as part of S12.2.1
(trailing stop commit `7c00073`). The config, broker logic (both `paper_broker.py`
and `kraken_client.py`), and mutual exclusion guard in `risk_manager.py` were all
included in that commit. Only the test file was missing.

**Implementation:** `tests/test_breakeven_stop.py` — 3 unit tests:

| Test | Scenario |
|------|----------|
| `test_sl_moves_to_entry_when_trigger_reached` | Price ≥ entry × (1 + trigger_pct/100) → SL moves to entry_price |
| `test_sl_does_not_move_before_trigger` | Price below threshold → SL unchanged |
| `test_sl_does_not_re_fire_once_at_entry` | SL already at entry → guard prevents double-fire |

All 3 tests pass (`pytest tests/test_breakeven_stop.py`).

**Config reference:**
```yaml
breakeven_stop:
  enabled: false   # mutually exclusive with trailing_stop
  trigger_pct: 2.0 # move SL to entry when gain >= 2%
```

**Broker logic location:** `src/exchange/paper_broker.py` → `check_stops_and_tp()`,
branch `elif breakeven_cfg.get("enabled", False)` (lines ~322–338).
Guard condition: `pos["stop_loss_price"] < pos["entry_price"]` ensures single-fire.

---
