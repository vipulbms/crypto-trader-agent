# Session Notes — 2026-04-11 (Part G)

## Changes

### 1. Add `trailing_stop` and `correlation_clusters` steps to add-pair skill

**Bug report / Feature request:** After the 10-pair addition session (#145–#154), both `trailing_stop.per_pair_overrides` and `risk.correlation_clusters` had to be updated retroactively. Neither step was mentioned in the add-pair skill, causing them to be missed.

**Root cause / Motivation:** `.claude/skills/add-pair/SKILL.md` step 5 only covered 8 sub-items (a–h). It had no guidance on trailing stop tier selection or cluster assignment.

**Fix / Implementation:**

- **Step 5(i): `trailing_stop.per_pair_overrides`** — added a volatility-tier table (standard / high-vol L1 / meme / extreme meme) with explicit trail_pct / activate_after_pct values. Rule: standard pairs (BTC, ETH, XRP etc.) need no override; high-vol and meme pairs get a wider trailer.

- **Step 5(j): `risk.correlation_clusters`** — added the current cluster table (6 clusters: large_cap_l1, memecoins, alt_l1, payment_legacy, eth_l2, ai_tokens), decision rules for add-to-existing / create-new / leave-unclustered, and YAML snippets for both cases.

- **Checklist:** two new items appended:
  - `trailing_stop.per_pair_overrides` updated — or confirmed global default (5%/3%) is appropriate
  - `risk.correlation_clusters` reviewed — pair added to existing cluster, new cluster created, or left unclustered with rationale

- **Issue #160 closed:** config.yaml correlation_clusters update was already applied in commit b8dbf4c (session_2026_04_11f). Issue closed retroactively.

**Files changed:**
- `.claude/skills/add-pair/SKILL.md` — step 5(i), step 5(j), two checklist items

---
