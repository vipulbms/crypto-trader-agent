# Session Notes — 2026-04-17 (Part D)

## Changes

### 1. Chore: raise max_position_pct from 20% to 30% (#265)

**Request:** Increase the maximum position size cap from 20% to 30% of portfolio value per trade.

**Files changed:**
- `config.yaml` `trading.max_position_pct`: 20 → 30
- `config.yaml` `position_sizing.max_position_pct`: 20 → 30 (hard cap in adaptive sizing; kept aligned)
- `config.yaml` `risk.drawdown_recovery.max_position_pct_override`: 10 → 15 (remains half of normal)
- `src/agent/prompts.py` `build_cycle_prompt()`: hardcoded label `(20% of $...)` → `(30% of $...)`
- `.claude/skills/trading-rules/SKILL.md`: drawdown recovery bullet `× 10%` → `× 15%`, updated description to "half of normal 30%"

**Notes:**
- `base_position_pct` (16%) intentionally left unchanged — this is the base before signal-strength/volatility scaling, not the ceiling
- Drawdown recovery cap remains half of normal (15% of 30%)
- 309 tests pass (no test changes needed — config-only + label change)
