# Session Notes — 5 April 2026 (Part G)

## Changes

### 1. README.md — Full Rewrite

**Bug report / Feature request:** README was stale — still referenced `deepseek-r1:7b` and Ollama exclusively, and was missing all quantitative features added in the Volatility-Adaptive Quant Migration (sessions a–f).

**Root cause / Motivation:** The previous README described the original signal-gate architecture. After the quant migration, minimum profit floor, kill switch, OBI, and limit orders were added, the README no longer matched the system.

**Fix / Implementation:** Full rewrite of `README.md`:
- Features section updated to list all new capabilities: confluence scoring, ATR sizing, dynamic TP, OBI, limit orders, profit floor, circuit breaker, kill switch, time-of-day filter, volume dead zone, heartbeat, healthcheck webhook.
- Pair table updated with correct TP% for all 15 pairs; added note that TP is ATR-adjusted per trade.
- Prerequisites updated: `python3.11 -m venv` instead of pyenv path; LLM section expanded to show Ollama, Gemini, and generic provider options.
- Configuration reference table added covering all config.yaml parameters.
- Architecture section added with data-flow description.
- Troubleshooting, backtesting, and CLI usage sections added.

---

### 2. docs/business_requirements.md — Full Rewrite to Formal BRD

**Bug report / Feature request:** `docs/business_requirements.md` was written as a loose architecture notes document. The product owner requested a formal Business Requirements Document following a standard BRD template.

**Root cause / Motivation:** The original v1.0 format lacked numbered functional requirements, traceability, glossary, and the formal structure expected by a BRD.

**Fix / Implementation:** Complete rewrite to formal BRD v2.0:
- Standard header table (version, author, date, status).
- Revision history table (v1.0 through v2.0).
- 13 numbered sections: Executive Summary, Business Objectives, Stakeholders, Scope, Assumptions, Constraints, Functional Requirements (FR-01 to FR-99), Non-Functional Requirements, Business Rules (BR-01 to BR-15), Pair Configuration Table, Out-of-Scope, Acceptance Criteria, Glossary (23 terms).
- All 15 trading pairs listed with TP/SL targets.
- All risk thresholds formally codified as Business Rules.
- Old `business-requirement.md` (root) deleted — `docs/business_requirements.md` is now the single source of truth.

---

### 3. docs/codebase.md — New File (Developer Reference)

**Bug report / Feature request:** No developer reference existed for the current codebase. New contributors had no single document covering module responsibilities, function signatures, DB schema, or config parameters.

**Root cause / Motivation:** Generated using the `/explain` skill pattern (read each module, document it, synthesise into a reference). As of this session, all 13 modules have been documented.

**Fix / Implementation:** Created `docs/codebase.md` (744 lines):
- Module-by-module reference: `indicators.py`, `signals.py`, `features.py`, `websocket_feed.py`, `paper_broker.py`, `kraken_client.py`, `risk_manager.py`, `prompts.py`, `tools.py`, `trading_agent.py`, `audit_logger.py`, `notifier.py`, `database.py`.
- Every public function's purpose, parameters, and return type documented.
- Database schema section (all 6 tables across `audit.db`, `paper_trading.db`, `live_trading.db`).
- `config.yaml` parameter reference table (~30 parameters with defaults and descriptions).
- Data flow section (end-to-end cycle: WS feed → indicators → signals → LLM → risk → broker → audit).
- Design Patterns section (6 patterns: Interface Parity, JIT Price, SKILL.md injection, DB-backed state, Candle Timestamp, Deterministic Veto).

---

### 4. docs/how_to_debug.md — New File (Debug Guide)

**Bug report / Feature request:** No operational runbook existed for debugging misbehaving trades or unexpected HOLD streaks.

**Root cause / Motivation:** Gaps between signal generation, risk vetoes, and LLM decisions are hard to trace without knowing which DB table/log to query.

**Fix / Implementation:** Created `docs/how_to_debug.md` (616 lines):
- 5-step debug workflow (check logs → check audit_signals → check risk rejections → check LLM decisions → check broker execution).
- SQL snippets for all common queries (recent signals, top rejections, trade history, balance drift).
- Log grep patterns for LLM calls, WS reconnects, risk rejections.
- Paper vs live debugging differences highlighted.
- Common failure scenarios with root cause and fix checklist.

---

### 5. commit SKILL.md — Step 4 and Step 8 Expanded

**Bug report / Feature request:** The commit skill only referenced 3 docs (`business-requirement.md`, `README.md`, `plan.md`). After adding 6 documentation files, the commit workflow did not instruct updating them.

**Root cause / Motivation:** Step 4 was written before `docs/codebase.md`, `docs/how_to_debug.md`, `docs/detailed_solution_design.md`, and `docs/epics_stories_ac.md` existed.

**Fix / Implementation:**
- Step 4 rewritten to cover all 6 docs with per-file update guidance and a Minor/Moderate/Large complexity guide.
- Step 8 stage list updated to explicitly include all 6 docs with the caveat "only stage if actually modified".

---

### 6. CLAUDE.md — Model Reference Generalised

**Bug report / Feature request:** CLAUDE.md "Known Behaviours / Gotchas" section still referenced `deepseek-r1` specifically for the `<think>` block behaviour.

**Root cause / Motivation:** The LLM is now model-agnostic; the note applies to any reasoning model.

**Fix / Implementation:** One-line update — changed "deepseek-r1 `<think>` blocks" to "Reasoning model `<think>` blocks" with examples `(e.g. DeepSeek-R1, QwQ)`.

---

### 7. plan.md — Model Label Updated

**Bug report / Feature request:** Architecture diagram in `plan.md` still showed `deepseek-r1:7b` as the LLM label.

**Fix / Implementation:** Label updated to `(tool-capable)` to reflect model-agnostic design.

---
